```
git clone https://github.com/Adek2k4/flask-admin-blog.git
cd flask-admin-blog
python -m venv venv
pip install flask
python -m flask --app flaskr init-db
python -m flask --app flaskr run --debug
```
